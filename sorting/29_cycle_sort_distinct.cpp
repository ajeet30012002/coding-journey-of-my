#include <bits/stdc++.h>
using namespace std;
void cycle_sort(int* a,int size)
{
    int item=a[cs]
}
int main()
{
    int size;
    cout << "\nEnter the size of the arrays" << endl;
    cin >> size;
    int a[size];
    cout << "\nEnter the elements of the arrays " << endl;
    for (int i = 0; i < size; i++)
    {
        cin >> a[i];
    }
    cycle_sort(a,size);
    return 0;
}
